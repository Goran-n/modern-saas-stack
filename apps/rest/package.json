{
  "name": "@figgy/rest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "doppler run -p figgy-be -c dev -- bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun",
    "start": "doppler run -p figgy-be -c production -- bun run dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@figgy/communication": "workspace:*",
    "@figgy/config": "workspace:*",
    "@figgy/shared-db": "workspace:*",
    "@figgy/utils": "workspace:*",
    "hono": "^4.8.9"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
