{
  "name": "@kibly/communication",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo",
    "test": "bun test",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@kibly/config": "workspace:*",
    "@kibly/file-manager": "workspace:*",
    "@kibly/nlq": "workspace:*",
    "@kibly/shared-db": "workspace:*",
    "@kibly/utils": "workspace:*",
    "twilio": "^5.7.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
