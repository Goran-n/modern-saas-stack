export interface MainProps {
  /**
   * Whether to add default padding
   * @default true
   */
  padding?: boolean;

  /**
   * Whether to make it full height
   * @default false
   */
  fullHeight?: boolean;

  /**
   * Additional CSS classes
   */
  class?: string;
}
