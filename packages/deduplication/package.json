{
  "name": "@kibly/deduplication",
  "version": "1.0.0",
  "description": "Anti-duplication system for files and documents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@kibly/config": "workspace:*",
    "@kibly/shared-db": "workspace:*",
    "@kibly/utils": "workspace:*"
  },
  "devDependencies": {
  }
}